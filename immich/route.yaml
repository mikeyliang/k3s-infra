apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: immich-route
  namespace: app-internal
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: infrastructure
      sectionName: websecure
  hostnames:
    - "photos.mikey-liang.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: immich-server
          port: 3001
